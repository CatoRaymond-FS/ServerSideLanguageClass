const uppercase = (str) => {
    return str.toUpperCase();
}
//lowercase function to lowercase a string input
const lowercase = (str) => {
    return str.toLowerCase();
}

module.exports = {
    uppercase,
    lowercase
}